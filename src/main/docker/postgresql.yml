version: '2'
services:
    projectone-postgresql:
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/projectone/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=projectone
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
